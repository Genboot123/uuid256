{
  "name": "uuid256-examples",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "browser",
    "bun",
    "edge",
    "node"
  ],
  "scripts": {
    "install:all": "turbo run install",
    "test": "turbo run test",
    "e2e": "turbo run e2e",
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
