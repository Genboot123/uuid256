{
  "name": "uuid256-examples",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/examples/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "e2e": "turbo run e2e",
    "lint": "turbo run lint",
    "fmt": "turbo run fmt"
  },
  "devDependencies": {
    "turbo": "^2.1.1"
  }
}